import { ComplaintWorkingVendor } from './complaint-working-vendor';

describe('ComplaintWorkingVendor', () => {
  it('should create an instance', () => {
    expect(new ComplaintWorkingVendor()).toBeTruthy();
  });
});
